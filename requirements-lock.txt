# Exact package versions of the environment in which every number in the paper
# was verified to reproduce (Python 3.11). requirements.txt gives the ranges;
# install these pins to match the verification run exactly:
#   pip install -r requirements-lock.txt
numpy==2.3.5
pandas==3.0.2
scipy==1.17.1
statsmodels==0.15.0
matplotlib==3.10.9
ephem==4.2.1
# Optional (not pinned): basemap -- used only for the Caribbean locator inset in
# the fig. S1 site map; the script skips the inset with a notice if absent.
